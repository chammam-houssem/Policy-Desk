You are working on a GitHub Pages website using a single-page HTML architecture with TailwindCSS, Chart.js, and PapaParse.

Context

The current file energy-explorer.html:

Uses TailwindCSS (CDN) for layout and styling
Uses Chart.js for data visualization
Uses PapaParse for CSV data loading
Has a monolithic structure (HTML + inline styles + JS)
Uses a card-based layout with a centered page container
Includes custom CSS variables and typography (Inter + Roboto Serif)

You will redesign this page, not rebuild from scratch.

Objective

Implement a split-view interactive layout that introduces a dual-panel comparison interface, while preserving:

Existing data logic (PapaParse + Chart.js)
Existing dataset structure
Core visualization components
Core UI Change

Replace the current single-column layout with a two-panel horizontal split interface:

Layout Requirements
Full-width container (max-w removed or expanded significantly)
Two side-by-side panels:
Left panel
Right panel
Panels separated by a thin, subtle vertical divider
Divider style:
Very low contrast
Soft edge
No harsh borders
Background contrast:
Slight variation between panels (e.g. white vs #f9fafb, or subtle tint)
Must remain minimal and clean
Panel Behavior

Each panel should:

Be structurally identical
Contain:
Country selector
Chart container
Metadata / indicators (if present in current design)
Operate independently
Share the same dataset but allow different selections
Interaction Model
User can:
Select different countries per panel
Compare energy mix / emissions side-by-side
Charts update independently per panel
No global state override between panels
Refactoring Constraints
DO NOT:
Do not rewrite data loading logic unless necessary
Do not remove Chart.js or PapaParse
Do not introduce frameworks (React, Vue, etc.)
Do not modularize into multiple files
DO:
Reuse existing functions where possible
Duplicate and scope logic cleanly for left/right panels
Introduce minimal abstraction only if needed for clarity
Visual Design Rules
Maintain current typography system
Preserve existing color variables (:root)
Reduce visual weight:
Lighter borders
Softer backgrounds
Less “card-heavy” feel
Panels should feel like:
A continuous surface split in two, not two separate cards
Implementation Steps
Restructure Layout
Replace main container with flex/grid split layout
Create left-panel and right-panel
Duplicate UI Components
Country selectors
Chart canvas elements
Labels / metadata
Scope IDs and State
Ensure each panel has unique IDs
Example:
chart-left, chart-right
country-select-left, country-select-right
Adapt Chart Logic
Instantiate two Chart.js instances
Bind each to its respective panel
Ensure updates do not conflict
Style Divider
Implement subtle vertical separator
Avoid hard borders
Use opacity or gradient if needed
Apply Background Contrast
Slight variation between panels
Keep neutral and minimal
Output

Modify the existing energy-explorer.html file directly.

Return:

Full updated HTML file
Clean, readable structure
No unnecessary comments or explanations

If anything is ambiguous, prioritize:

Preserving current functionality
Clean separation of panel logic
Minimal, precise UI changes
